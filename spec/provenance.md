# Provenance

- **2026-08-17** ChatGPT voice thread (not published here). Temporary name AODL.
- Same day: HOTL 0.1 (`t_7432ab2d`) then HOTL 0.2 (`t_83991e68`) on groot.
- Upstream: [NousResearch/hermes-agent#88589](https://github.com/NousResearch/hermes-agent/issues/88589) (open, P3, `needs-decision`).
- 2026-09-10: notes landed in `frontier-kb`; this repository publishes the IR, schema, and the validator/fixtures the 0.2 README listed but never attached.
- Same day: encodings join table + living-night catalog; harness catalog (`hermes` `omp` `o8` `grok` `codex` `claude` `pi` `fx`) and OSS network map.
- Same day: C(RAID) named hybrid from `kvnloo/blueprint` CR/CA/CI/CD (`spec/craid.md`). Unlabeled hybrid stays `not-inferred`. Working note (not arXiv): `docs/working-note.md`.

Do not file a duplicate GitHub issue. Do not paste the private transcript into this repo. Extend HOTL 0.2 rather than inventing a second spec.

`kvnloo/flow` is a Ratatui swarm TUI, not this calculus. zerOS `AgentCoreLanguage.jsx` is badge UI. firstmate is a crew distro, not this IR.
